# CHIP-8-emulator

To run it, you need to run the _python/main.py_ in one terminal and the _html/index.html_ in the browser (both simultaneously). It because the server will start a server that will comunicate with the html page by the JavaScript (websocket) [...]
